var expect = require('expect');

// formats output to be more readable
describe('App', () => {
  /*
    describes what the test should do
    first takes string, the second takes assertions
  */
  it('should properly run tests', () => {
    expect(1).toBe(1); // expect one to be equal to one
  });
});
